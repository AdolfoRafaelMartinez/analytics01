<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blockdaemon Blockchain Height</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Blockdaemon <%= network.charAt(0).toUpperCase() + network.slice(1) %> Blockchain Height</h1>
        <% if (height) { %>
            <p>The current blockchain height is: <%= height %></p>
        <% } else if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
        <a href="/">Back to Home</a>
    </div>
</body>
</html>
