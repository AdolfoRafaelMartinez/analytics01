<!DOCTYPE html>
<html>
<head>
    <title>Address from Private Key</title>
    <link rel="stylesheet" type="text/css" href="/styles.css" />
</head>
<body>
    <div class="container">
        <a href="/">Home</a>
        <h1>Derive Address from Private Key</h1>
        <p>Enter a private key in WIF format to calculate its public key and the corresponding address.</p>
        
        <form method="POST" action="/private-key-to-address">
            <div class="form-group">
                <label for="network_name">Network:</label>
                <select name="network_name" id="network_name" class="form-input">
                    <option value="mainnet">Mainnet</option>
                    <option value="testnet">Testnet</option>
                    <option value="testnet4" selected>Testnet4</option>
                </select>
            </div>

            <div class="form-group">
                <label for="privateKey">Private Key (WIF):</label>
                <input type="text" id="privateKey" name="privateKey" class="form-input" placeholder="Enter your WIF private key">
            </div>
    
            <button type="submit" class="btn-submit">Derive Address</button>
        </form>

        <% if (address) { %>
            <div class="result-container">
                <h2>Derived Information</h2>
                <p><strong>Address:</strong> <%= address %></p>
            </div>
        <% } %>

        <% if (error) { %>
            <div class="error-container">
                <h2>Error</h2>
                <p><%= error %></p>
            </div>
        <% } %>
    </div>
</body>
</html>